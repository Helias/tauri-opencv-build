name: Release macOS Intel

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version (e.g., v1.0.0)"
        required: true
        type: string
        default: "1.0.0"

permissions:
  contents: write

jobs:
  build-macos-intel:
    runs-on: macos-15-intel
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create or update Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Check if release already exists
          if gh release view "${{ github.event.inputs.version }}" --json isDraft 2>/dev/null; then
            echo "Release ${{ github.event.inputs.version }} already exists, reusing it"
          else
            echo "Creating new draft release ${{ github.event.inputs.version }}"
            gh release create "${{ github.event.inputs.version }}" \
              --draft \
              --title "Release ${{ github.event.inputs.version }}" \
              --notes "Release ${{ github.event.inputs.version }}"
          fi

      # - name: Cache Homebrew packages
      #   uses: actions/cache@v4
      #   with:
      #     path: |
      #       ~/Library/Caches/Homebrew
      #       /usr/local/Cellar/opencv
      #       /usr/local/Cellar/llvm
      #       /usr/local/Cellar/pkg-config
      #       /usr/local/Cellar/ninja
      #       /usr/local/Cellar/z3
      #       /usr/local/opt/opencv
      #       /usr/local/opt/llvm
      #       /usr/local/opt/pkg-config
      #       /usr/local/opt/ninja
      #       /usr/local/opt/z3
      #     key: ${{ runner.os }}-${{ runner.arch }}-brew-${{ hashFiles('.github/workflows/release-macos-intel.yml') }}
      #     restore-keys: |
      #       ${{ runner.os }}-${{ runner.arch }}-brew-

      - name: Install dependencies
        run: |
          # Only install packages that aren't already cached
          for package in opencv llvm pkg-config ninja z3 cmake; do
            if ! brew list $package &>/dev/null; then
              echo "Installing $package..."
              brew install $package
            else
              echo "$package already installed, skipping"
            fi
          done

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      # - name: Rust cache
      #   uses: swatinem/rust-cache@v2
      #   with:
      #     workspaces: "./src-tauri -> target"
      #     cache-on-failure: false
      #     key: macos-15-intel-x86_64-apple-darwin-${{ hashFiles('**/Cargo.lock', '**/Cargo.toml') }}

      - name: Install frontend dependencies
        run: npm install

      - name: Create Cargo config with ORT environment variables
        shell: bash
        run: |
          mkdir -p .cargo
          cat > .cargo/config.toml << 'EOF'
          [env]
          ORT_STRATEGY = "compile"
          ORT_COMPILE_TARGET = "x86_64-apple-darwin"
          ORT_COMPILE_VERSION = "1.19.2"
          ORT_USE_SYSTEM_LIBSTDCXX = "0"
          EOF

          echo "Created .cargo/config.toml"
          cat .cargo/config.toml

      - name: Verify ONNX Runtime configuration
        run: |
          echo "=== ONNX Runtime Build Configuration ==="
          echo "Strategy: compile from source"
          echo "Target: x86_64-apple-darwin"
          echo "Version: 1.19.2"
          echo ""
          echo "ONNX Runtime will be compiled from source during cargo build"
          echo "This may take 15-20 minutes - please be patient"
        shell: bash

      - name: Set OpenCV environment variables
        run: |
          echo "PKG_CONFIG_PATH=$(brew --prefix opencv)/lib/pkgconfig:$PKG_CONFIG_PATH" >> $GITHUB_ENV
          echo "DYLD_LIBRARY_PATH=$(brew --prefix opencv)/lib:$(brew --prefix llvm)/lib:$(brew --prefix z3)/lib:$DYLD_LIBRARY_PATH" >> $GITHUB_ENV
          echo "LIBCLANG_PATH=$(brew --prefix llvm)/lib" >> $GITHUB_ENV
          echo "$(brew --prefix llvm)/bin" >> $GITHUB_PATH

          # Let opencv-rust use pkg-config to find OpenCV libraries
          # Don't set OPENCV_LINK_LIBS manually as it causes linker issues
          # pkg-config will provide the correct library names and paths
        shell: bash

      - name: Build Tauri app with explicit environment variables
        run: |
          echo "=== Building with ORT environment variables ==="
          echo "ORT_STRATEGY=compile"
          echo "ORT_COMPILE_TARGET=x86_64-apple-darwin"
          echo "ORT_COMPILE_VERSION=1.19.2"
          echo ""
          echo "Running Tauri build..."
          ORT_STRATEGY=compile \
          ORT_COMPILE_TARGET=x86_64-apple-darwin \
          ORT_COMPILE_VERSION=1.19.2 \
          ORT_USE_SYSTEM_LIBSTDCXX=0 \
          npm run tauri build -- --target x86_64-apple-darwin

      - name: Upload artifacts to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Find and upload the generated artifacts
          for file in src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg; do
            if [ -f "$file" ]; then
              gh release upload "${{ github.event.inputs.version }}" "$file" --clobber
            fi
          done

          for file in src-tauri/target/x86_64-apple-darwin/release/bundle/macos/*.app.tar.gz; do
            if [ -f "$file" ]; then
              gh release upload "${{ github.event.inputs.version }}" "$file" --clobber
            fi
          done
